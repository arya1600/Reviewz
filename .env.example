# Copy to .env for local dev. On Railway, add the same keys as service variables.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# OpenAI — set in Supabase Edge Function secrets, not here:
#   supabase secrets set OPENAI_API_KEY=sk-...
