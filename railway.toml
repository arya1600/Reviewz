# Railway — https://docs.railway.com/guides/react
# Connect this repo in Railway, set VITE_* variables, then deploy.

[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
